import express from 'express'
// import {
//     createConvsersation,
//     getConvsersation,
//     getSingleConvsersation,
//     updateConvsersation
// } from '../controllers/conversation.controller';
// import verifyToken from '../middlewares/jwt';

const conversationRouter = express.Router()

// conversationRouter.get('/', verifyToken, getConvsersation);
// conversationRouter.post  ('/', verifyToken, createConvsersation);
// conversationRouter.get('/single/:id', verifyToken, getSingleConvsersations);
// conversationRouter.put('/:id', verifyToken, updateConvsersation);

export default conversationRouter